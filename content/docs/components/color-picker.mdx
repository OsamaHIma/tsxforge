---
title: ColorPicker
description: A Popover-based color selection component.
---

import ColorPicker from "@/components/tsxforge/color-picker";
import { ColorPickerBasicDemo, ColorPickerDisabledDemo } from "@/components/tsxforge/color-picker-demo";

# ColorPicker

The `ColorPicker` component allows users to select a color from a Chrome-style picker. It is built using a Popover and integrates seamlessly with the design system.

## Installation

<Tabs items={["Command", "Manual"]}>
  <Tab value="Command">
    <Tabs items={["pnpm", "npm", "yarn", "bun"]}>
      <Tab value="pnpm">
        ```bash
        pnpm dlx shadcn@latest add https://tsxforge.vercel.app/registry/color-picker.json
        ```
      </Tab>
      <Tab value="npm">
        ```bash
        npx shadcn@latest add https://tsxforge.vercel.app/registry/color-picker.json
        ```
      </Tab>
      <Tab value="yarn">
        ```bash
        npx shadcn@latest add https://tsxforge.vercel.app/registry/color-picker.json
        ```
      </Tab>
      <Tab value="bun">
        ```bash
        bunx shadcn@latest add https://tsxforge.vercel.app/registry/color-picker.json
        ```
      </Tab>
    </Tabs>
  </Tab>
  <Tab value="Manual">
    <Steps>
      <Step>
        Install the following dependencies:
        ```bash
        npm install react-color
        ```
      </Step>
      <Step>
        Install the following Shadcn UI components:
        ```bash
        npx shadcn@latest add popover label
        ```
      </Step>
      <Step>
        Copy the component code to `src/components/tsxforge/color-picker.tsx`
      </Step>
    </Steps>
  </Tab>
</Tabs>

## Usage

Interactive color selection with real-time feedback.

<Tabs items={['Preview', 'Code']}>
  <Tab value="Preview">
    <div className="not-prose p-4">
      <ColorPickerBasicDemo />
    </div>
  </Tab>
  <Tab value="Code">
```tsx
import { ColorPicker } from "@/components/tsxforge/color-picker"
import { useState } from 'react'

export default function Example() {
  const [color, setColor] = useState('#3b82f6')

  return (
    <ColorPicker
      label="Theme Color"
      value={color}
      onChange={setColor}
    />
  )
}
```
  </Tab>
</Tabs>

## Disabled State

The picker can be disabled to prevent user interaction.

<Tabs items={['Preview', 'Code']}>
  <Tab value="Preview">
    <div className="not-prose p-4">
      <ColorPickerDisabledDemo />
    </div>
  </Tab>
  <Tab value="Code">
```tsx
<ColorPicker
  label="Theme Color"
  defaultValue="#3b82f6"
  disabled={true}
/>
```
  </Tab>
</Tabs>

## Features

- **Controlled & Uncontrolled**: Supports both `value` (controlled) and `defaultValue` (uncontrolled) for flexible integration.
- **Popover Integration**: Saves screen real estate by opening the picker in a toggleable overlay.
- **ChromePicker**: Familiar interface for precise color selection.
- **Accessibility**: Includes optional label support with `htmlFor` matching.

## Custom Props

| Prop | Type | Default | Description |
| ---- | ---- | ------- | ----------- |
| `value` | `string` | `undefined` | The current hex color value (controlled). |
| `defaultValue` | `string` | `"#000000"` | The initial color value (uncontrolled). |
| `onChange` | `(color: string) => void` | `undefined` | Callback triggered when a new color is selected. |
| `label` | `string` | `undefined` | Optional label text displayed above the picker button. |
| `disabled` | `boolean` | `false` | Whether the color picker is disabled. |
| `id` | `string` | `undefined` | Unique identifier for the component. |
| `className` | `string` | `undefined` | Additional CSS classes for the container. |
